import { Brand, Movies, SpecialCategories } from "../../types";

export interface TemplateProps {
  translatedText: any;
  brand: Brand;
  htmlContent: string;
  specialCategories: SpecialCategories;
}
